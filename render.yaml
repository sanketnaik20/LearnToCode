services:
  # --- Backend Service ---
  - type: web
    name: learntocode-api
    env: docker
    dockerContext: ./backend
    dockerCommand: "" # Uses CMD from Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true

  # --- Frontend Service ---
  - type: web
    name: learntocode-app
    env: docker
    dockerContext: ./frontend
    dockerCommand: "" # Uses CMD from Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: learntocode-api
          property: host
